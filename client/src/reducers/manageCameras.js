export default function ManageCameras(state = {
  cameras: [],
}, action) {
  // switch (action.type) {
  //   case 'ADD_BAND':
  //     return { ...state, bands: [...state.bands, action.band] }
  //   default:
  //     return state;
  // }
};
